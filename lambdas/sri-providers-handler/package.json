{
    "name": "providers-handler",
    "version": "1.0.0",
    "description": "Handles provider operations",
    "main": "index.js",
    "scripts": {
        "build": "tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {},
    "devDependencies": {
        "esbuild": "^0.20.0",
        "@types/aws-lambda": "^8.10.0",
        "@types/node": "^18.0.0",
        "typescript": "^5.0.0"
    }
}